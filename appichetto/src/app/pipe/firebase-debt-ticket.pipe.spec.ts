import { FirebaseDebtTicketPipe } from './firebase-debt-ticket.pipe';

describe('FirebaseDebtTicketPipe', () => {
  it('create an instance', () => {
    const pipe = new FirebaseDebtTicketPipe();
    expect(pipe).toBeTruthy();
  });
});
